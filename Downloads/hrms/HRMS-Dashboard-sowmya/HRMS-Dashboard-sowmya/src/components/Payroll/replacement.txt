<div className="flex gap-1 pb-0">
                  <div className="w-[30px] flex-shrink-0"></div> {/* Placeholder for edit button column */}
                  {["M","T","W","T","F","S","S","M","T","W","T","F","S","S","M","T","W","T","F","S","S","M","T"].map((day, i) => (
                    <div key={i} className="w-6 h-6 flex items-center justify-center flex-shrink-0">
                      <span className="text-xs font-medium text-[#666666]">{day}</span>
                    </div>
                  ))}
                </div>
                <div className="flex gap-1 pb-0">
                  <div className="w-[30px] flex-shrink-0"></div> {/* Placeholder for edit button column */}
                  {Array.from({ length: 30 }, (_, i) => String(i + 1).padStart(2, "0")).map((date, i) => (
                    <div key={i} className="w-6 h-6 flex items-center justify-center flex-shrink-0">
                      <span className="text-xs text-[#333333]">{date}</span>
                    </div>
                  ))}
                </div>
